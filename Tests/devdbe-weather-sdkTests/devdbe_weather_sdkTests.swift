import XCTest
@testable import devdbe_weather_sdk

final class devdbe_weather_sdkTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(devdbe_weather_sdk().text, "Hello, World!")
    }
}
